package edu.ntnu.idatt2106.krisefikser.persistance.enums;

/**
 * This file defines the RequestType enum, which is used to categorize the type of requests in the
 * application.
 */
public enum RequestType {
  JOIN_REQUEST,
  INVITATION
}
